language: android
jdk: oraclejdk8
sudo: required

install: ./gradlew assemble
script: ./gradlew check

android:
  components:
    - tools
    - platform-tools
    - build-tools-25.0.1
    - android-23

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
